{
  "name": "loneliness-support-companion",
  "version": "1.0.0",
  "private": true,
  "description": "A supportive-listening companion with mood check-ins and crisis-safety handling. Not therapy, not roleplay.",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "test": "node --test tests/"
  },
  "dependencies": {}
}
